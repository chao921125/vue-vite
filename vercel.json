{
"buildCommand": "npm run build",
"outputDirectory": "dist",
"devCommand": "npm run dev",
"installCommand": "corepack enable && corepack prepare pnpm@11.1.3 --activate && pnpm install --no-frozen-lockfile",
"framework": "vite"
}
