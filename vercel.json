{
	"buildCommand": "npm run build",
	"outputDirectory": "dist",
	"devCommand": "npm run dev",
	"installCommand": "corepack enable && corepack use pnpm@11.1.3 && pnpm install --no-frozen-lockfile",
	"framework": "vite"
}
