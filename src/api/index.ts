import user from "./modules/user";
import common from "./modules/common";
import mock from "./modules/mock";

/**
 * url *
 * method
 * data
 * params get
 */

const api = {
	user: user,
	common: common,
	mock: mock,
};

export default api;
